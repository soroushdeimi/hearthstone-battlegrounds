class Player:
    def __init__(self , hp , gold , board):
        hp = 30
        gold = 0
        board  = []

    def add_minion():
        if(i <= 7):
            self.board.extend();
        else:
            print("unvalid")
    def remove_minion(n):
            self.board.remove(n)
    def take_damage(n)

